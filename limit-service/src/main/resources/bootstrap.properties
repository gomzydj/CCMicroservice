#name of the application should be same as config properties file name in the config server
spring.application.name=limits-services
spring.cloud.config.uri=http://localhost:8888
spring.profiles.active=qa